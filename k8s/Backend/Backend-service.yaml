apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: security-tracker
spec:
  type: NodePort       # MUST be here
  selector:
    app: backend
  ports:
    - port: 3000        # internal cluster port
      targetPort: 3000  # container port
      nodePort: 30001   # port on host
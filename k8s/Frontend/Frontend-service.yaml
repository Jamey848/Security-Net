apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: security-tracker
spec:
  selector:
    app: frontend
  ports:
  - port: 8080        # port used internally in cluster
    targetPort: 8080  # port your pod is listening on
    nodePort: 30080   # optional if you want to access from host
  type: NodePort